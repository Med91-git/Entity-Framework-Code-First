using Microsoft.EntityFrameworkCore;

namespace EntityFramework___Code_First
{
    internal class Program
    {
        
        static void Main(string[] args)
        {

            // Instancier le serveur de BDD avec la BDD (classe de context) 

            // Utiliser mot clé using pour utiliser le serveur de la BDD  

            // Instancier les entités/classes (les tables de la BDD)
            // Instancier le calcul des comptes epargnes de la personne (facultatif)

        }
    }
}
